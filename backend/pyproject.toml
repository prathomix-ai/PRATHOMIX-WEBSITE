[tool.ruff]
target-version = "py311"
line-length = 100
select = ["E", "W", "F", "I"]
ignore = ["E501", "F401"]

[tool.ruff.isort]
known-first-party = ["api", "database", "middleware", "utils"]

[tool.pyright]
pythonVersion = "3.11"
typeCheckingMode = "basic"
